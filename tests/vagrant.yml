---
# test file for nano
- hosts: all
  remote_user: vagrant
  sudo: true
  roles:
    - nano
  vars:
    nano_nanorc_destinations:
      skell:
        dest: /etc/skel
      current:
        dest: "{{ ansible_env.HOME }}"
      vagrant:
        dest: /home/vagrant
        owner: vagrant
        group: vagrant
